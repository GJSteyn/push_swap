#include "libft.h"
#include "get_next_line.h"
#include "push_swap.h"

int		main(int arc, char **arv)
{
	int		i;

	i = 1;
	if (!args_valid(arc, arv))
	{
		ft_putstr_fd("Error\n", 2);
		return (0);
	}
	return (0);
}
